## 新功能

- “关于”页新增“软件更新”：首次进入自动检查 GitHub 最新正式版本，也可手动重试。
- 有新版时可点击“下载并更新”，显示下载进度，校验完成后自动替换程序并重启。
- 保留现有配置、驱动与快捷键设置；旧程序会备份，替换或启动失败时保留或尝试恢复旧版。

## 更新说明

- 自动更新只安装更高版本的 Windows x64 正式版，需要网络连接及程序目录写入权限。
- 首次使用请下载完整的 ZIP 包；已有用户也可下载 EXE 覆盖更新。
